--- a/src/quickshell/bar/modules/WorkspacesWidget.qml
+++ b/src/quickshell/bar/modules/WorkspacesWidget.qml
@@ -95,6 +95,7 @@
                     let data = JSON.parse(this.text);
                     let wsList = data.workspaces || [];
                     let winList = data.windows || [];
+                    let currentScreen = (barWindow && barWindow.screen && barWindow.screen.name) ? barWindow.screen.name : "";
                     let occ = {};
                     for (let i = 0; i < winList.length; i++) {
                         let win = winList[i];
@@ -102,19 +103,25 @@
                             occ[win.workspace_id] = true;
                         }
                     }
-                    let activeIdx = 0;
+                    let activeIdx = -1;
+                    let targetOcc = {};
                     for (let j = 0; j < wsList.length; j++) {
                         let w = wsList[j];
+                        if (currentScreen !== "" && w.output && w.output !== currentScreen) {
+                            continue;
+                        }
                         let idx = (w.idx !== undefined ? w.idx : (w.id !== undefined ? w.id : 1)) - 1;
-                        if (w.is_focused || w.is_active) {
+                        if (w.is_focused || (w.is_active && activeIdx === -1)) {
                             activeIdx = idx;
                         }
                         if (w.active_window_id !== null || occ[w.id] || occ[w.idx]) {
-                            occ[idx] = true;
+                            targetOcc[idx] = true;
                         }
                     }
-                    workspacesWidgetRoot.niriActiveIndex = activeIdx;
-                    workspacesWidgetRoot.niriOccupiedMap = occ;
+                    if (activeIdx >= 0) {
+                        workspacesWidgetRoot.niriActiveIndex = activeIdx;
+                    }
+                    workspacesWidgetRoot.niriOccupiedMap = targetOcc;
                 } catch (e) {}
 
                 niriWaiter.running = false;
@@ -131,7 +138,7 @@
         command: [
             "bash",
             "-c",
-            "niri msg --json event-stream 2>/dev/null | grep -m 1 -E '\"(WorkspacesChanged|WorkspaceActivated|WindowOpenedOrChanged|WindowClosed|WindowFocusChanged)\"'"
+            "niri msg --json event-stream 2>/dev/null | grep -m 1 -E '\"(WorkspacesChanged|WorkspaceActivated|WindowsChanged|WindowOpenedOrChanged|WindowClosed|WindowFocusChanged)\"'"
         ]
         onExited: {
             niriPoller.running = false;
@@ -329,7 +336,7 @@
                 property var ws: workspacesWidgetRoot.wsForId(wsId)
                 property bool isOccupied: {
                     if (workspacesWidgetRoot.isNiri) {
-                        return !workspacesWidgetRoot.niriOccupiedMap[index];
+                        return !!workspacesWidgetRoot.niriOccupiedMap[index];
                     }
                     if (workspacesWidgetRoot.isSway) {
                         return !!workspacesWidgetRoot.swayOccupiedMap[index];
--- a/src/quickshell/bar/sidemodules/SideWorkspacesWidget.qml
+++ b/src/quickshell/bar/sidemodules/SideWorkspacesWidget.qml
@@ -95,6 +95,7 @@
                     let data = JSON.parse(this.text);
                     let wsList = data.workspaces || [];
                     let winList = data.windows || [];
+                    let currentScreen = (barWindow && barWindow.screen && barWindow.screen.name) ? barWindow.screen.name : "";
                     let occ = {};
                     for (let i = 0; i < winList.length; i++) {
                         let win = winList[i];
@@ -102,19 +103,25 @@
                             occ[win.workspace_id] = true;
                         }
                     }
-                    let activeIdx = 0;
+                    let activeIdx = -1;
+                    let targetOcc = {};
                     for (let j = 0; j < wsList.length; j++) {
                         let w = wsList[j];
+                        if (currentScreen !== "" && w.output && w.output !== currentScreen) {
+                            continue;
+                        }
                         let idx = (w.idx !== undefined ? w.idx : (w.id !== undefined ? w.id : 1)) - 1;
-                        if (w.is_focused || w.is_active) {
+                        if (w.is_focused || (w.is_active && activeIdx === -1)) {
                             activeIdx = idx;
                         }
                         if (w.active_window_id !== null || occ[w.id] || occ[w.idx]) {
-                            occ[idx] = true;
+                            targetOcc[idx] = true;
                         }
                     }
-                    sideWsRoot.niriActiveIndex = activeIdx;
-                    sideWsRoot.niriOccupiedMap = occ;
+                    if (activeIdx >= 0) {
+                        sideWsRoot.niriActiveIndex = activeIdx;
+                    }
+                    sideWsRoot.niriOccupiedMap = targetOcc;
                 } catch (e) {}
 
                 niriWaiter.running = false;
@@ -131,7 +138,7 @@
         command: [
             "bash",
             "-c",
-            "niri msg --json event-stream 2>/dev/null | grep -m 1 -E '\"(WorkspacesChanged|WorkspaceActivated|WindowOpenedOrChanged|WindowClosed|WindowFocusChanged)\"'"
+            "niri msg --json event-stream 2>/dev/null | grep -m 1 -E '\"(WorkspacesChanged|WorkspaceActivated|WindowsChanged|WindowOpenedOrChanged|WindowClosed|WindowFocusChanged)\"'"
         ]
         onExited: {
             niriPoller.running = false;
@@ -332,7 +339,7 @@
                 property var ws: sideWsRoot.wsForId(wsId)
                 property bool isOccupied: {
                     if (sideWsRoot.isNiri) {
-                        return !sideWsRoot.niriOccupiedMap[index];
+                        return !!sideWsRoot.niriOccupiedMap[index];
                     }
                     if (sideWsRoot.isSway) {
                         return !!sideWsRoot.swayOccupiedMap[index];

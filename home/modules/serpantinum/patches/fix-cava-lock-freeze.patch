diff -urN a/src/quickshell/lock/Lock.qml b/src/quickshell/lock/Lock.qml
--- a/src/quickshell/lock/Lock.qml	2026-08-31 19:44:47.421897415 +0700
+++ b/src/quickshell/lock/Lock.qml	2026-08-31 19:45:13.510709252 +0700
@@ -675,11 +675,14 @@
                         }
                     }
 
+                    property bool isCavaSubscribed: false
                     function updateCavaConsumer() {
-                        if (screenRoot.wingsReveal > 0.98) {
+                        if (screenRoot.wingsReveal > 0.98 && !isCavaSubscribed) {
                             Cava.registerConsumer();
-                        } else {
+                            isCavaSubscribed = true;
+                        } else if (screenRoot.wingsReveal <= 0.98 && isCavaSubscribed) {
                             Cava.unregisterConsumer();
+                            isCavaSubscribed = false;
                         }
                     }
 
@@ -982,7 +985,10 @@
                     }
 
                     Component.onDestruction: {
-                        Cava.unregisterConsumer();
+                        if (isCavaSubscribed) {
+                            Cava.unregisterConsumer();
+                            isCavaSubscribed = false;
+                        }
                         if (isSysSubscribed) {
                             SysData.unsubscribe();
                             isSysSubscribed = false;
diff -urN a/src/quickshell/media/MusicPopup.qml b/src/quickshell/media/MusicPopup.qml
--- a/src/quickshell/media/MusicPopup.qml	2026-08-31 19:44:47.426409299 +0700
+++ b/src/quickshell/media/MusicPopup.qml	2026-08-31 19:45:13.511111779 +0700
@@ -39,33 +39,45 @@
         chargeAnim.restart();
     }
 
+    property bool isCavaSubscribed: false
+
     onVisibleChanged: {
         if (visible) {
             forceActiveFocus();
             resetAndPlayIntro();
-            Cava.registerConsumer();
+            if (!isCavaSubscribed) {
+                Cava.registerConsumer();
+                isCavaSubscribed = true;
+            }
             if (!eqProc.running) eqProc.running = true;
             if (titleTextMain.implicitWidth > titleClipRect.width) {
                 marqueeContainer.x = 0;
                 titleAnim.restart();
             }
         } else {
-            Cava.unregisterConsumer();
+            if (isCavaSubscribed) {
+                Cava.unregisterConsumer();
+                isCavaSubscribed = false;
+            }
             titleAnim.stop();
             marqueeContainer.x = 0;
         }
     }
 
     Component.onCompleted: {
-        if (visible) {
+        if (visible && !isCavaSubscribed) {
             forceActiveFocus();
             resetAndPlayIntro();
             Cava.registerConsumer();
+            isCavaSubscribed = true;
         }
     }
 
     Component.onDestruction: {
-        Cava.unregisterConsumer();
+        if (isCavaSubscribed) {
+            Cava.unregisterConsumer();
+            isCavaSubscribed = false;
+        }
     }
 
     property int barCount: 60
diff -urN a/src/quickshell/widgets/faces/MusicFace.qml b/src/quickshell/widgets/faces/MusicFace.qml
--- a/src/quickshell/widgets/faces/MusicFace.qml	2026-08-31 19:44:47.429873386 +0700
+++ b/src/quickshell/widgets/faces/MusicFace.qml	2026-08-31 19:45:13.511205345 +0700
@@ -33,11 +33,20 @@
 
     property bool isVisVisible: visible && showBars
 
-    onIsVisVisibleChanged: {
-        if (isVisVisible) Cava.registerConsumer();
-        else Cava.unregisterConsumer();
+    property bool isSubscribed: false
+
+    function updateSubscription() {
+        if (isVisVisible && !isSubscribed) {
+            isSubscribed = true;
+            Cava.registerConsumer();
+        } else if (!isVisVisible && isSubscribed) {
+            isSubscribed = false;
+            Cava.unregisterConsumer();
+        }
     }
 
+    onIsVisVisibleChanged: updateSubscription()
+
     function updateVisibility() {
         if (height >= 65 && width >= 180) showArt = true;
         else if (height <= 58 || width <= 170) showArt = false;
@@ -54,11 +63,14 @@
 
     Component.onCompleted: {
         updateVisibility();
-        if (isVisVisible) Cava.registerConsumer();
+        updateSubscription();
     }
 
     Component.onDestruction: {
-        if (isVisVisible) Cava.unregisterConsumer();
+        if (isSubscribed) {
+            isSubscribed = false;
+            Cava.unregisterConsumer();
+        }
     }
 
     onHeightChanged: updateVisibility()
diff -urN a/src/quickshell/widgets/faces/MusicFaceRound.qml b/src/quickshell/widgets/faces/MusicFaceRound.qml
--- a/src/quickshell/widgets/faces/MusicFaceRound.qml	2026-08-31 19:44:47.433443526 +0700
+++ b/src/quickshell/widgets/faces/MusicFaceRound.qml	2026-08-31 19:45:13.511248360 +0700
@@ -23,17 +23,29 @@
 
     property bool isVisVisible: visible
 
-    onIsVisVisibleChanged: {
-        if (isVisVisible) Cava.registerConsumer();
-        else Cava.unregisterConsumer();
+    property bool isSubscribed: false
+
+    function updateSubscription() {
+        if (isVisVisible && !isSubscribed) {
+            isSubscribed = true;
+            Cava.registerConsumer();
+        } else if (!isVisVisible && isSubscribed) {
+            isSubscribed = false;
+            Cava.unregisterConsumer();
+        }
     }
 
+    onIsVisVisibleChanged: updateSubscription()
+
     Component.onCompleted: {
-        if (isVisVisible) Cava.registerConsumer();
+        updateSubscription();
     }
 
     Component.onDestruction: {
-        if (isVisVisible) Cava.unregisterConsumer();
+        if (isSubscribed) {
+            isSubscribed = false;
+            Cava.unregisterConsumer();
+        }
     }
 
     property int barCount: 64
